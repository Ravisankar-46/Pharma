export default function ManufacturerDashboardPage({ profile, returns, navigate }) {
  const incoming = returns.filter((item) => item.status === 'RECEIVED')
  return <div className="page-stack manufacturer-page"><section className="manufacturer-identity"><p className="eyebrow">Manufacturer workspace</p><h1>{profile?.manufacturerName || 'ABC Pharma'}</h1><p>{profile?.name || 'Manufacturer operations team'}</p><span>{profile?.address || 'Facility address not listed'}</span></section><div className="page-heading"><div><p className="eyebrow">Reverse chain</p><h2>Choose an operation</h2><p className="page-subtitle">Verify received batches, record disposal, and retrieve certificates.</p></div></div><section className="manufacturer-launch-grid"><button type="button" onClick={() => navigate('/manufacturer/returns')}><strong>Incoming Returns</strong><span>{incoming.length} distributor-received batch{incoming.length === 1 ? '' : 'es'} awaiting review.</span><b>Open incoming returns →</b></button><button type="button" onClick={() => navigate('/manufacturer/disposal')}><strong>Disposal</strong><span>Track accepted batches and disposal evidence.</span><b>Open disposal →</b></button><button type="button" onClick={() => navigate('/manufacturer/certificates')}><strong>Certificates</strong><span>View PharmaLoop destruction certificates.</span><b>View certificates →</b></button></section></div>
}

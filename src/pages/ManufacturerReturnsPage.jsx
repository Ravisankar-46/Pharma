function statusLabel(status) { return status.replaceAll('_', ' ') }

export default function ManufacturerReturnsPage({ returns, navigate }) {
  return <div className="page-stack manufacturer-page"><div className="page-heading"><div><p className="eyebrow">Manufacturer intake</p><h1>Incoming returns</h1><p className="page-subtitle">Only distributor-received returns are eligible for manufacturer processing.</p></div></div><section className="panel manufacturer-table-panel"><div className="panel-heading"><div><p className="eyebrow">Shared return records</p><h2>Awaiting manufacturer verification</h2></div><span className="count-pill">{returns.length} records</span></div><div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Batch</th><th>Distributor</th><th>Expected</th><th>Received</th><th>Status</th><th /></tr></thead><tbody>{returns.map((item) => <tr key={item.id}><td><strong>{item.medicineName}</strong><small>{item.strength}</small></td><td><code>{item.batchNumber}</code></td><td><small>{item.distributorName || item.distributorId || 'Distributor'}</small></td><td><strong>{item.quantity}</strong><small>units</small></td><td><strong>{item.receivedQuantity ?? '-'}</strong><small>units</small></td><td><span className="manufacturer-status">{statusLabel(item.status)}</span></td><td><button type="button" className="table-action distributor-action" onClick={() => navigate(`/manufacturer/returns/${item.id}`)}>View return</button></td></tr>)}</tbody></table>{!returns.length && <div className="empty-state"><strong>No distributor-received returns yet</strong><p>Completed distributor receipts will appear here.</p></div>}</div></section></div>
}
